# [PROJECT_NAME] — Shared vocabulary

Optional glossary, written only when the interview surfaced non-trivial
domain vocabulary. A trivial domain skips this file entirely (no
placeholder file is left behind).

| Term | Definition | Notes |
| --- | --- | --- |
| [TERM_1] | [DEFINITION_1] | [e.g. first used in ARCHITECTURE.md §Modules] |
| [TERM_2] | [DEFINITION_2] | |

Rules:

- This file is **pure vocabulary**: the project's own words with tight
  definitions. No implementation details, no spec prose, no scratch notes.
- A term lands here the moment it resolves during the interview — it is
  the shared language every later document and conversation reuses without
  re-deriving it.
- When a term is redefined later, update this file and the referencing
  documents in the same change.
