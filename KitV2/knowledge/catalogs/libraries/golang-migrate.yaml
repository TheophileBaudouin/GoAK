---
id: source:go:golang-migrate
title: golang-migrate SQL migration reference
kind: Source
version: 1
status: active
owner: go-agent-kit
tags: [go, database, migrations, sql, third-party]
go_version: "1.25+"
dependencies: []
last_verified: 2026-08-03
source: https://github.com/golang-migrate/migrate
selection: >-
  Consider for versioned, reproducible SQL schema migrations when a consumer
  explicitly needs migration tooling for production deployments. It supports
  many database drivers and can run as a CLI or as a library.
limits:
  - >-
    This pointer is not a product recommendation and has no runnable recipe;
    verify the current release, driver support, and migration ordering
    semantics before adoption.
  - >-
    Migrations are stateful by nature; the consumer project must own its
    migration history, checksums, and rollback strategy.
relationships:
  references:
    - https://github.com/golang-migrate/migrate
