# Template catalog contract

```yaml
name: go-agent-template-catalog
status: catalog
purpose: Define project shapes without claiming unimplemented templates are runnable.
validation: Each promoted template adds a Go module, executable scenario, test, and evidence.
```
