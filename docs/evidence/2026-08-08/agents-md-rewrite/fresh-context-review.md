All verification steps complete. Here is the review.

## Review

**Scope reviewed:** `KitV2/AGENTS.md` (rewrite, uncommitted working-tree change), `18-zone-agents.md` §9, `23-zone-ui-kit.md` §4, `validate-kitv2.py` `check_agents_md_contract` + 5 unit tests, root `AGENTS.md`, `.pi/memory/Agent.md`, `.agent/sync-ui-kit-from-upstream.sh`, `.pi/prompts/update-ui-kit.md`, `ui-kit/AGENTS.md` (sha256), `.pi/docs/GOAK.md`.

- **Correct — protocol compliance (Z9 §9.1):** the rewrite's section order matches the canonical order exactly: Identity → `## Normative levels` (lines 8–26, legend + vocabulary) → User guide marker (28–36) → `## Non-Negotiable Rules` (38–54) → `## Repository map` (56–70) → `## Task Routing` (72–96) → Project Foundation marker (98–114) → `## UI work — Wails projects` + sha256 (116–150) → `## Memory` (152–164) → `## Validation` (166–193) → `## Limits` (195–206) → closing invariants echo (208–210). Normative vocabulary is strictly MUST/MUST NOT/SHOULD/MAY; no lowercase "should", "prefer", or "best practices" anywhere (grep-verified); the only "strongly recommended" is the sanctioned marker exception (line 132). Bullets are atomic (one decision each). No history: the former `workflow-clarify → plan → tasks → implement → verify` reference is gone (old file line 112); grep for `removed|former|replaces|2026` returns zero hits. The UI section is a condensed delegation; the dropped "Where to find what" table, shadcn/npx command block, and registration paragraph all exist in the delegation target (`ui-kit/AGENTS.md` lines 15–16, 30, 49–50, 64–71).
- **Correct — hard-rule survival (old vs new):** routing mandatory + pre-loaded tool guidelines + on-demand catalog fiches (§Task Routing); frozen base never duplicated/modified + never "improve" + new named component + sourced justification + index entry (lines 125–127); static-only Wails-bound (hash routing, `base: "./"`, `@wailsio/runtime` + bindings, no SSR/`next/*`, lines 129–130); English only (44 + 141); sourced rules (139); no silent duplication (134); SDK autonomy (138); diagnostics pass before done (143–144); memory inventory + never assume + `Decisions.md` not bootstrapped (152–164); PARTIAL status table (177–185); complete 10-command gate incl. `KITV2_STRICT_CATALOG` (168–176); limits table (197–206); ask-before-dependency/manifest (49 + echo); **no invention** — new explicit invariant (line 44), stronger than before; no false "passed" claims (45 + echo). Every hard rule of the previous file survived; none silently dropped.
- **Correct — markers:** all three marker sections intact; `workspace-init` section byte-identical to the previous version; user-guide marker preserved (with a "should"→"MUST" strengthening, an improvement per §9.2.4); the UI marker carries `ae432ca832839f98ba80eea058f626880facd77ab6392152bf7d191a8f36f1ad`, which equals `shasum -a 256 KitV2/ui-kit/AGENTS.md` (verified by command).
- **Correct — no metaproject markers:** grep for `.agent/`, `KIT_CHARTER`, `metaproject`, `D-20xx-xx-xx-NN`, `KVA-NNN`, `KitV2/`, `Z1x`/`Zx —`/`A1`/`C2`/`N1`, `../` → zero hits. No `kind:domain:slug` prose ids; every referenced id (`recipe-desktop-app`, `kit-resource-routing`, `spec-driven-dev`, `deep-discuss`, `workflow-memory`, `workspace-init`) resolves in `router/index.json` or `.pi/skills|prompts`.
- **Correct — guardrails coherent:** Z9 §9 (9.1–9.4 + DoD) is a complete protocol; Z13 §4 rewritten to delegation semantics ("revised 2026-08-08, D-2026-08-08-19"); sync helper updated consistently (lines 286–307: FAIL on stale marker with re-verify + refresh instruction); `update-ui-kit.md` Phase 2 step 2 matches; root AGENTS.md rule (121–136) and Agent.md (222–234) mirror each other; `check_agents_md_contract` enforces structure + 16 KiB budget (warning >12 KiB) + 3 history-marker regexes, with 5 targeted unit tests (pass/fail/lost-section/over-budget/history/near-budget); all 34 validator tests pass.
- **Correct — gate:** `validate-kitv2.py` PASS, `validate-cognitive.py` PASS (C12 prose-id gate), `validate-instructions.py` PASS, `templates/_kit-ci-workflow.yml` exists (referenced in Validation), GOAK.md untouched by the diff and covers every command (no nonexistent command documented, none dropped).

**Fixed:** nothing — read-only review, no edits made.

**Blocker:** none.

**Worth-fixing:** none.

**Notes (nits):**
- `KitV2/AGENTS.md:121` — "the kit recommends initializing the project foundation" inside the preserved workspace-init marker: "recommends" is a near-synonym outside the exact sanctioned wording (§9.2.4 sanctions only "strongly recommended, never a hard gate"). Pre-existing and inside a marker section; the gate-vs-recommendation intent it encodes is exactly what §9.2.4 protects. Optional reword on a future marker edit.
- `GOAK.md` (pre-existing, untouched by this diff) never mentions `deep-discuss`, and its §3.7 gate omits the `KITV2_STRICT_CATALOG=1` line that `KitV2/AGENTS.md`'s Validation section carries. Not a regression from the rewrite; AGENTS.md itself documents no nonexistent command.
- Z9 §9.1 says "renaming one [section title] fails `check_agents_md_contract`", but the mechanical check asserts only heading presence (7 headings) and marker/title substrings — section *order* and the exact canonical titles (User guide/Project Foundation carry subtitles) are review-enforced, not mechanically checked. The contract text slightly overstates the mechanical guarantee; acceptable as a review control (C2 allows it), worth a wording tweak if precision matters.

## Acceptance Report

**criterion-1: satisfied** — concrete, evidence-backed findings returned with file paths/line numbers and severities above (5 verifiable checks, each with command or file evidence).