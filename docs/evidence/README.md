# Evidence

Raw command outputs and probe results belong here. Each change should use a
dated subdirectory and preserve command, exit code, environment, and observable
scenario output verbatim. These files are prepared for version control; the
current workspace has no Git repository, so VCS versioning remains BLOCKED.

Current evidence covers standalone KitV2 and v1 archive/restore checkpoints.
Historical v1 product evidence was retired with the deleted product; the
external archive is the rollback referent.
